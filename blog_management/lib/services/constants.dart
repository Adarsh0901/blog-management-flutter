enum FilterOptions {
  ascending,
  descending,
  date,
  none
}

enum imageOptions {
  network,
  file
}

const appTitle = 'Blog Management';
const baseUrl = 'https://blog-management-flutter-default-rtdb.firebaseio.com';
const blogs = 'blogsList';